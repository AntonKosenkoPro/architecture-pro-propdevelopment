apiVersion: v1
kind: Pod
metadata:
  name: insecure-hostpath
  namespace: audit-zone
spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - mountPath: /host-data
      name: host-volume
  volumes:
  - name: host-volume
    hostPath:
      path: /etc
